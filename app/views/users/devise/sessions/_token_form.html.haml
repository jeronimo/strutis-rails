= form_with url: user_session_path, method: :post, id: 'auth-form', data: { turbo: false } do
  %input.form-control.my-2{ type: 'text', name: 'user[authentication_token]', placeholder: '...', required: true }
  %button.btn.btn-primary.w-100.my-2{ type: 'submit' } Sign In
