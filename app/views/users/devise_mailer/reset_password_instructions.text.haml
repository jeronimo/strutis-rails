Hi #{@user.full_name.presence || @user.email},

Someone has requested a link to change your Strutis password. You can do this through the link below:

= edit_user_password_url(reset_password_token: @token)

If you didn't request this, ignore this email. Your password won't change until you access the link above and create a new one.
