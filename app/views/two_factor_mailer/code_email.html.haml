%p Hi #{@user.full_name.presence || @user.email},
%p Your Strutis sign-in code is:
%p{ style: 'font-size: 28px; letter-spacing: 6px; font-weight: 700; margin: 16px 0;' }
  = @code
%p It expires in #{@expiry_minutes} minutes. If you did not try to sign in, ignore this email.
